{
  "name": "ink_testing",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "client": "bun build src/client.ts --outdir public --target browser --format esm --minify --sourcemap=inline --watch",
    "server": "bun --watch run src/server.tsx",
    "dev": "bun run client & bun run server"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "ink": "^6.0.1",
    "react": "^19.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
